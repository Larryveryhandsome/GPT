# GPT

This repository contains a static webpage with information on real estate contracts. To view it locally, open `index.html` in your browser or serve the folder with a web server such as:

```bash
python3 -m http.server 8000
```

Then navigate to `http://localhost:8000` in your browser.
